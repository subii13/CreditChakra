# Root-level environment reference. Copy the relevant values into
# backend/.env and frontend/.env — never commit real values anywhere.
# See backend/.env.example and frontend/.env.example for the actual
# per-app variables consumed by each app.

NODE_ENV=development
